{
  "$schema": "../../agents/schemas/agent.schema.json",
  "version": "1.14.0",
  "name": "project-planner",
  "displayName": "Project Planner Agent",
  "description": "Reads a GitHub Project's README (goal statement) and the current issue landscape via a 3-step linear chain (goal-extract → issue-survey → plan), then emits proposed_issues[] (deferred_items-compatible) to close the gap between the goal and the current state.",
  "parameters": {
    "issue": {
      "type": "number",
      "description": "GitHub Issue number (sentinel issue for the project)",
      "required": true,
      "cli": "--issue"
    }
  },
  "runner": {
    "flow": {
      "systemPromptPath": "prompts/system.md",
      "prompts": {
        "registry": "steps_registry.json"
      }
    },
    "verdict": {
      "type": "count:iteration",
      "config": {
        "maxIterations": 6
      }
    },
    "logging": {
      "directory": "tmp/logs/agents/project-planner",
      "format": "jsonl",
      "maxFiles": 100
    }
  }
}
