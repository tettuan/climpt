{
  "$schema": "../../agents/schemas/agent.schema.json",
  "version": "1.14.0",
  "name": "project-planner",
  "displayName": "Project Planner Agent",
  "description": "Reads a GitHub Project's README (goal statement) and the current issue landscape, then emits issue candidates (deferred_items) to close the gap between the goal and the current state.",
  "parameters": {
    "issue": {
      "type": "number",
      "description": "GitHub Issue number (sentinel issue for the project)",
      "required": true,
      "cli": "--issue"
    }
  },
  "runner": {
    "flow": {
      "systemPromptPath": "prompts/system.md",
      "prompts": {
        "registry": "steps_registry.json"
      }
    },
    "verdict": {
      "type": "count:iteration",
      "config": {
        "maxIterations": 3
      }
    },
    "logging": {
      "directory": "tmp/logs/agents/project-planner",
      "format": "jsonl",
      "maxFiles": 100
    }
  }
}
