# Breakdown Configuration for iterator-steps
params:
  two:
    directiveType:
      pattern: "^(initial|continuation|section|retry|complete)$"
    layerType:
      pattern: "^(issue|project|iterate|externalState)$"
