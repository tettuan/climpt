# Breakdown Configuration
params:
  two:
    directiveType:
      pattern: "^(build|convert-skill|create|migrate|name)$"
    layerType:
      pattern: "^(c3l-command|frontmatter|instruction|skill|to-plugin)$"
