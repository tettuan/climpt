# Breakdown Configuration for triage-recovery-steps
# directiveType must include all c2 values used in steps_registry.json
# layerType must include all c3 values used in steps_registry.json
params:
  two:
    directiveType:
      pattern: "^(closure)$"
    layerType:
      pattern: "^(recover)$"
