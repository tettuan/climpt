{
  "$schema": "../../agents/schemas/agent.schema.json",
  "version": "1.0.0",
  "name": "ci-recheck",
  "displayName": "CI Recheck Agent",
  "description": "Validator that observes a PR's CI status, posts a state-mutating recheck comment, and emits a verdict (green | red | timeout | still-running) that routes phase transition. Recovery-Resume hop placed between merger and merge-ready to break merger's self-cycle on ci-pending.",
  "parameters": {
    "prNumber": {
      "type": "number",
      "description": "GitHub PR number whose CI status to recheck",
      "required": true,
      "cli": "--pr-number"
    }
  },
  "runner": {
    "flow": {
      "systemPromptPath": "prompts/system.md",
      "prompts": {
        "registry": "steps_registry.json"
      }
    },
    "verdict": {
      "type": "count:iteration",
      "config": {
        "maxIterations": 1
      }
    },
    "logging": {
      "directory": "tmp/logs/agents/ci-recheck",
      "format": "jsonl",
      "maxFiles": 100
    }
  }
}
