# 07: Custom Variables

**What:** Tests that `--uv-*` user variables are forwarded to prompts. **Why:**
Variable substitution is central to prompt customization.

## Verifies

- `--uv-target` output contains target-related content
- Echo input output contains the piped string
