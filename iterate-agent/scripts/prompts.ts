/**
 * Iterate Agent - System Prompt Builder
 *
 * Note: buildSystemPrompt() has been removed.
 * System prompts are now built via breakdown CLI in loadSystemPromptViaC3L().
 *
 * Initial and continuation prompts are handled by CompletionHandler
 * implementations in the completion/ directory.
 */

// This file is kept for backward compatibility reference.
// No exported functions remain.
